# Codespaces用オーバーライド。
# docker-compose.ymlに重ねて使い、appサービスだけ開発用イメージに切り替える。
# devcontainer.jsonのdockerComposeFileで自動的に読み込まれる。

services:
  app:
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    volumes:
      - ./backend:/workspace
    working_dir: /workspace