# Backend
DATABASE_URL=postgres://postgres:postgres@db:5432/studytracker?sslmode=disable

# マイグレーション自動実行(開発時はtrue推奨)
# 本番相当の検証時は docker-compose.prod.yml を重ねて使うとfalseになり、
# 専用のmigrateコンテナが事前に1回だけ実行する運用に切り替わる。
AUTO_MIGRATE=true

# Frontend (Oauthコールバック後のリダイレクト先、CORS許可オリジンにも使用)
FRONTEND_URL=http://localhost:5173

# Google OAuth
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret-here
GOOGLE_REDIRECT_URL=http://localhost:8080/api/auth/google/callback

# Session
SESSION_SECRET=change_me_in_production

# Email (通知用)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=

# LINE通知（未実装。実装後に有効化）
LINE_CHANNEL_ACCESS_TOKEN=
LINE_CHANNEL_SECRET=

# Stripe（Phase 2、有料プラン導入時）
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
